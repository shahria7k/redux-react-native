import { View, Text } from "react-native";
import React from "react";
import tw from "twrnc";
const Login = () => {
	return (
		<View style={tw`h-1/2 border-2 border-black m-2 bg-white`}>
			<Text>This is Login</Text>
		</View>
	);
};

export default Login;
